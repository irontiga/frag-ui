// Blockchain data...e.g. current block height and account info
const INITIAL_STATE = {}

export default (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state
    }
}

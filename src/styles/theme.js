import { css } from 'lit-element'

export const theme = css`
    * {
        /* Day/night mode */
        --background-color: #333;
        --text-color: #fff;

        /* Theme color */
        --primary-color: red;
        --secondary-color: green;
    }
`
